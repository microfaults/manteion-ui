# Manteion API base URL (no trailing slash)
VITE_MANTEION_URL=http://localhost:9090

# Grafana base URL for dashboard embeds (d-solo panels)
VITE_GRAFANA_URL=http://localhost:3000

# Default environment (kustomize overlay) — maps to X-Faults-Lab-Environment header
VITE_DEFAULT_ENV=online-boutique
